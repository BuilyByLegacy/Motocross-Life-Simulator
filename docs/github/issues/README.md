# GitHub Issue Drafts

Issue draft files that have already been imported into GitHub are archived in
`completed/`.

The archived drafts were imported on 2026-07-08 after deduping against existing
open and closed GitHub issues. The import created issues through #207.

## App Store v1.0 create-now batch

For the current launch push, create real GitHub Issues only from:

```text
docs/github/issues/00_CREATE_NOW_V1_LAUNCH_BLOCKERS.md
```

All other issue draft files in this folder, including archived drafts, remain design backlog unless explicitly promoted. Do not import the entire folder for the v1.0 launch-blocker pass.
